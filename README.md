# jupyter-notebooks

Always use the local `venv/` in this directory when running Jupyter here — do not use a system or other environment's Python.

## Setup (already done)

```
python3 -m venv venv
source venv/bin/activate
pip install jupyter notebook
```

## Usage

```
source venv/bin/activate
jupyter notebook   # or: jupyter lab
```
